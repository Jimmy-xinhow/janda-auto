{
  "name": "janda-auto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node scripts/validate-content.mjs",
    "build": "bundle exec jekyll build",
    "dev": "bundle exec jekyll serve --livereload --host 127.0.0.1 --port 3333"
  },
  "devDependencies": {
    "yaml": "^2.9.0"
  }
}
